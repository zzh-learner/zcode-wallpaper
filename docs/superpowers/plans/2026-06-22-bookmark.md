# 控制中心书签管理 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在控制中心加第四块面板「书签」：用户手动增删常用网址（名称+URL），点击书签经中转页 go.html 跳转访问外部站，并能通过浏览器后退回到控制中心。

**Architecture:** 纯前端 + 纯 localStorage（仿书架 shelf 范式）。`bookmark.js` 纯函数库（URL 校验/增删/中转 URL 生成）双导出（CommonJS + `window.__ccBookmark`）。中转页 `go.html` 自包含（内联 CSS/JS），二次校验 URL 协议白名单防 XSS。server 只加 1 行重定向（`/control/go` → `/control/go.html`），书签数据零服务端依赖。

**Tech Stack:** 原生 JS（无框架，对齐 control/ 现状）、Node `http`（server 测试）、localStorage（数据持久）。

**Spec:** `docs/superpowers/specs/2026-06-22-bookmark-design.md`

---

## 文件结构

**新增：**
- `control/lib/bookmark.js` — 书签纯函数库（URL 校验/规范化、中转 URL 生成、增删改查纯逻辑）。双导出 CommonJS + `window.__ccBookmark`。范式完全参照 `control/lib/shelf.js`。
- `control/go.html` — 中转页。自包含（内联 CSS/JS），不依赖 bookmark.js。显示 title+url + 前往/返回按钮，二次校验 URL。
- `test/bookmarktest.cjs` — bookmark.js 纯函数单测，仿 `test/shelftest.cjs` 风格。

**修改：**
- `lib/control-server.cjs` — 加 `/control/go` → 302 `/control/go.html` 重定向（1 行，紧挨现有 `/control` → `/control/` 重定向）。
- `control/index.html` — 加 `<div id="bookmark-panel">` 面板 + `<script src="lib/bookmark.js">`。
- `control/control.js` — 加 `renderBookmarks()` 渲染 + 添加/删除表单事件 + 书签点击跳 go.html。
- `control/control.css` — 加 `.wide-input`（书签输入框宽度）+ `.err`（错误消息红字）+ 书签 url 行省略。
- `package.json` — test 脚本追加 `bookmarktest.cjs`。
- `test/controlservertest.cjs` — 追加 `/control/go` → 302 + `/control/go.html` → 200 断言。

**关键实现约定（参照 shelftest 现有模式）：** `bookmark.js` 的 localStorage 读写函数（getBookmarks/addBookmark/removeBookmark）**不做 mock 注入单测**——shelf.js 也没这么做（shelftest 只测纯函数 resolveStaleBookId/shelfDiff/makeShelfEntry/bookId）。localStorage 读写靠真机验证。单测只覆盖**可纯函数化的逻辑**：`normalizeUrl`、`buildGoUrl`、`makeBookmarkEntry`、`bookmarkId`、`isAllowedProtocol`。这样实现更简单，且和项目现有测试边界一致（教训：对齐现有惯例，不引入 shelf 都没有的测试机制）。

---

## Task 1: bookmark.js 纯函数库骨架 + 双导出

**Files:**
- Create: `control/lib/bookmark.js`

- [ ] **Step 1: 创建 bookmark.js，写 normalizeUrl + 协议白名单 + 双导出骨架**

```js
// Bookmark library (spec §4/§5). Pure helpers for URL validation/normalization,
// go-url generation, bookmark entry construction. Dual export: CommonJS for Node
// tests + window.__ccBookmark for browser (mirrors control/lib/shelf.js).
//
// localStorage read/write (getBookmarks/addBookmark/removeBookmark) are browser-
// only and NOT unit-tested — mirrors shelf.js convention (shelftest only covers
// pure fns). They are verified by real-machine checklist (spec §9 第四层).

// Allowed URL protocols. http/https only — javascript:/data:/file: etc rejected
// (spec §5 决策 1, XSS defense, no exceptions).
function isAllowedProtocol(u) {
  return u.protocol === "http:" || u.protocol === "https:";
}

// normalizeUrl: validate + normalize user input. Returns {ok,url} or {ok:false,error}.
// (spec §4) Auto-prepends http:// when missing (NOT https:// — let browser handle
// http->https upgrade; this layer shouldn't assume target supports https).
function normalizeUrl(input) {
  var s = input == null ? "" : String(input).trim();
  if (!s) return { ok: false, error: "网址不能为空" };
  var withProto = s.indexOf("://") !== -1 ? s : "http://" + s;
  var parsed;
  try { parsed = new URL(withProto); }
  catch (e) { return { ok: false, error: "URL 无效" }; }
  if (!isAllowedProtocol(parsed)) return { ok: false, error: "只支持 http/https 网址" };
  return { ok: true, url: parsed.href };
}

// buildGoUrl: build the /control/go.html?... URL for a bookmark click. Both url
// and title are encodeURIComponent'd so & = # in URLs don't break query parsing.
function buildGoUrl(url, title) {
  var base = "/control/go.html?url=" + encodeURIComponent(url);
  if (title) base += "&title=" + encodeURIComponent(title);
  return base;
}

// bookmarkId: stable unique id. "bm_" + timestamp base36 + 2 random chars.
// NOT a URL hash (bookmarks can repeat URLs; hash collision would cause wrong
// delete). Timestamp+random guarantees uniqueness (spec §4).
function bookmarkId() {
  var rand = Math.random().toString(36).slice(2, 4);
  return "bm_" + Date.now().toString(36) + rand;
}

// makeBookmarkEntry: build a shelf-shape entry {id,title,url,createdAt}. Pure.
// title defaults to URL hostname when empty (spec §6: don't force user to type name).
function makeBookmarkEntry(input) {
  var title = (input.title || "").trim();
  if (!title) {
    try { title = new URL(input.url).hostname; } catch (e) { title = input.url; }
  }
  return { id: bookmarkId(), title: title, url: input.url, createdAt: Date.now() };
}

// ---- Browser-only localStorage ops (not unit-tested, mirror shelf.js) ----
function getBookmarks() {
  try { return JSON.parse(localStorage.getItem("zcode-control:bookmarks") || "[]"); }
  catch (e) { return []; }
}
function setBookmarks(arr) {
  try { localStorage.setItem("zcode-control:bookmarks", JSON.stringify(arr)); return true; }
  catch (e) { return false; }
}
function addBookmark(entry) {
  var arr = getBookmarks();
  arr.push(entry);  // NO dedup (spec §8 边界 6: user may want same host twice)
  return setBookmarks(arr);
}
function removeBookmark(id) {
  return setBookmarks(getBookmarks().filter(function (b) { return b.id !== id; }));
}

if (typeof module !== "undefined" && module.exports) {
  module.exports = { isAllowedProtocol: isAllowedProtocol, normalizeUrl: normalizeUrl,
    buildGoUrl: buildGoUrl, bookmarkId: bookmarkId, makeBookmarkEntry: makeBookmarkEntry,
    getBookmarks: getBookmarks, setBookmarks: setBookmarks, addBookmark: addBookmark,
    removeBookmark: removeBookmark };
}
if (typeof window !== "undefined") {
  window.__ccBookmark = { isAllowedProtocol: isAllowedProtocol, normalizeUrl: normalizeUrl,
    buildGoUrl: buildGoUrl, bookmarkId: bookmarkId, makeBookmarkEntry: makeBookmarkEntry,
    getBookmarks: getBookmarks, setBookmarks: setBookmarks, addBookmark: addBookmark,
    removeBookmark: removeBookmark };
}
```

- [ ] **Step 2: 冒烟验证文件能被 require（语法无误）**

Run: `node -e "var b=require('./control/lib/bookmark.js'); console.log(typeof b.normalizeUrl, typeof b.buildGoUrl)"`
Expected: `function function`

- [ ] **Step 3: Commit**

```bash
git add control/lib/bookmark.js
git commit -m "feat(bookmark): bookmark.js 纯函数库 (normalizeUrl/buildGoUrl/entry/localStorage)"
```

---

## Task 2: bookmarktest.cjs 单测（TDD——但 Task 1 已实现，这里钉死行为）

说明：项目 test 风格是"先实现再写 test 钉死"（shelftest 就是这模式，不是严格 TDD）。这里对齐——Task 1 已写实现，Task 2 写测试钉死所有边界。

**Files:**
- Create: `test/bookmarktest.cjs`

- [ ] **Step 1: 写 bookmarktest.cjs（覆盖 normalizeUrl 全部边界 + buildGoUrl + makeBookmarkEntry + bookmarkId）**

```js
// Test control/lib/bookmark.js pure functions (spec §9 第一层).
// Mirrors test/shelftest.cjs style. localStorage ops NOT tested (browser-only,
// verified by real-machine checklist) — same boundary as shelftest.
var bm = require("../control/lib/bookmark.js");
var pass = 0, fail = 0;
function check(n, c) { console.log((c ? "PASS ✓ " : "FAIL ✗ ") + n); c ? pass++ : fail++; }

// === normalizeUrl: protocol whitelist (XSS defense, most critical) ===
check("javascript: -> rejected", bm.normalizeUrl("javascript:alert(1)").ok === false);
check("data: -> rejected", bm.normalizeUrl("data:text/html,<script>").ok === false);
check("file: -> rejected", bm.normalizeUrl("file:///C:/x").ok === false);
check("vbscript: -> rejected", bm.normalizeUrl("vbscript:msgbox").ok === false);
check("blob: -> rejected", bm.normalizeUrl("blob:http://x/abc").ok === false);
check("ftp: -> rejected", bm.normalizeUrl("ftp://example.com").ok === false);

// === normalizeUrl: http/https accepted ===
check("github.com -> ok (auto-prepend http)", bm.normalizeUrl("github.com").ok === true);
check("github.com -> http://", bm.normalizeUrl("github.com").url === "http://github.com/");
check("https://github.com -> ok", bm.normalizeUrl("https://github.com").ok === true);
check("https preserved", bm.normalizeUrl("https://github.com").url === "https://github.com/");
check("http://  preserved", bm.normalizeUrl("http://example.com").url === "http://example.com/");

// === normalizeUrl: trim ===
check("whitespace trimmed", bm.normalizeUrl("  GitHub.com  ").url === "http://github.com/");

// === normalizeUrl: local addresses auto-prepend http ===
check("localhost:3000 -> http", bm.normalizeUrl("localhost:3000").url === "http://localhost:3000/");
check("127.0.0.1:8080 -> http", bm.normalizeUrl("127.0.0.1:8080").url === "http://127.0.0.1:8080/");

// === normalizeUrl: unicode/punycode accepted ===
check("unicode host -> ok", bm.normalizeUrl("http://中文.com").ok === true);

// === normalizeUrl: invalid inputs ===
check("empty string -> not ok", bm.normalizeUrl("").ok === false);
check("whitespace only -> not ok", bm.normalizeUrl("   ").ok === false);
check("not a url at all -> not ok", bm.normalizeUrl("not a url at all").ok === false);
check("null -> not ok", bm.normalizeUrl(null).ok === false);

// === buildGoUrl ===
check("buildGoUrl basic", bm.buildGoUrl("http://github.com/") ===
  "/control/go.html?url=" + encodeURIComponent("http://github.com/"));
check("buildGoUrl encodes & = #", bm.buildGoUrl("http://x.com/a?b=c&d=e").indexOf(encodeURIComponent("http://x.com/a?b=c&d=e")) !== -1);
check("buildGoUrl with title includes title param", bm.buildGoUrl("http://x.com/", "My Title").indexOf("&title=" + encodeURIComponent("My Title")) !== -1);
check("buildGoUrl without title omits title param", bm.buildGoUrl("http://x.com/", undefined).indexOf("title=") === -1);
check("buildGoUrl empty title omits title param", bm.buildGoUrl("http://x.com/", "").indexOf("title=") === -1);

// === bookmarkId: uniqueness + shape ===
var id1 = bm.bookmarkId(), id2 = bm.bookmarkId();
check("bookmarkId starts with bm_", /^bm_/.test(id1));
check("bookmarkId unique (2 calls differ)", id1 !== id2);

// === makeBookmarkEntry ===
var e1 = bm.makeBookmarkEntry({ title: "GitHub", url: "http://github.com/" });
check("makeBookmarkEntry: id present", typeof e1.id === "string" && /^bm_/.test(e1.id));
check("makeBookmarkEntry: title from input", e1.title === "GitHub");
check("makeBookmarkEntry: url from input", e1.url === "http://github.com/");
check("makeBookmarkEntry: createdAt is number", typeof e1.createdAt === "number");
// title defaults to hostname when empty
var e2 = bm.makeBookmarkEntry({ title: "", url: "http://github.com/" });
check("makeBookmarkEntry: empty title -> hostname", e2.title === "github.com");
var e3 = bm.makeBookmarkEntry({ title: "  ", url: "http://x.com/" });
check("makeBookmarkEntry: whitespace title -> hostname", e3.title === "x.com");
var e4 = bm.makeBookmarkEntry({ url: "http://y.com/" });
check("makeBookmarkEntry: missing title -> hostname", e4.title === "y.com");

// === isAllowedProtocol ===
check("isAllowedProtocol http true", bm.isAllowedProtocol({ protocol: "http:" }) === true);
check("isAllowedProtocol https true", bm.isAllowedProtocol({ protocol: "https:" }) === true);
check("isAllowedProtocol javascript false", bm.isAllowedProtocol({ protocol: "javascript:" }) === false);

console.log("\n" + pass + " passed, " + fail + " failed");
process.exit(fail === 0 ? 0 : 1);
```

- [ ] **Step 2: 运行测试，确认全绿**

Run: `node test/bookmarktest.cjs`
Expected: 全部 `PASS ✓`，最后一行 `N passed, 0 failed`，退出码 0。

- [ ] **Step 3: Commit**

```bash
git add test/bookmarktest.cjs
git commit -m "test(bookmark): bookmark.js 纯函数单测 (normalizeUrl 协议白名单/buildGoUrl/entry)"
```

---

## Task 3: server 加 /control/go 重定向

**Files:**
- Modify: `lib/control-server.cjs`（紧挨第 161 行 `/control` 重定向后加一行）

- [ ] **Step 1: 加 /control/go → 302 重定向**

在 `lib/control-server.cjs` 找到这段（约第 159-162 行）：

```js
      // root + control redirects (教训 18a: no-trailing-slash -> with slash)
      if (p === "/") { res.writeHead(302, { Location: "/control/" }); res.end(); return; }
      if (p === "/control") { res.writeHead(302, { Location: "/control/" }); res.end(); return; }
```

改为（加一行 `/control/go` 重定向，紧挨 `/control` 重定向）：

```js
      // root + control redirects (教训 18a: no-trailing-slash -> with slash)
      if (p === "/") { res.writeHead(302, { Location: "/control/" }); res.end(); return; }
      if (p === "/control") { res.writeHead(302, { Location: "/control/" }); res.end(); return; }
      // bookmark: /control/go (no .html) -> /control/go.html (same anti-trap as 教训 18a)
      if (p === "/control/go") { res.writeHead(302, { Location: "/control/go.html" }); res.end(); return; }
```

- [ ] **Step 2: 冒烟验证 server 语法**

Run: `node -e "require('./lib/control-server.cjs'); console.log('ok')"`
Expected: `ok`

- [ ] **Step 3: Commit**

```bash
git add lib/control-server.cjs
git commit -m "feat(bookmark): server 加 /control/go -> /control/go.html 重定向"
```

---

## Task 4: controlservertest 追加 go 重定向断言

**Files:**
- Modify: `test/controlservertest.cjs`（紧挨现有 `/control` → 302 断言后追加）

- [ ] **Step 1: 在 controlservertest.cjs 的 `/control` 重定向断言后，追加 /control/go 断言**

找到这段（约第 30-32 行）：

```js
    // /control (no slash) -> 302 /control/ (教训 18a)
    const redir = await httpReq("GET", base + "/control");
    check("/control -> 302", redir.status === 302);
    check("/control redirects to /control/", (redir.headers.location || "").indexOf("/control/") !== -1);
```

在其后追加（先准备 go.html 文件让静态托管能命中，再加断言）：

```js
    // bookmark: /control/go -> 302 /control/go.html (spec §3 server 改动)
    fs.writeFileSync(path.join(root, "control", "go.html"), "<!doctype html><title>go</title>");
    const goRedir = await httpReq("GET", base + "/control/go");
    check("/control/go -> 302", goRedir.status === 302);
    check("/control/go redirects to /control/go.html", (goRedir.headers.location || "").indexOf("/control/go.html") !== -1);
    // /control/go.html served as static (existing /control/ branch covers it)
    const goHtml = await httpReq("GET", base + "/control/go.html");
    check("/control/go.html -> 200 html", goHtml.status === 200 && goHtml.body.indexOf("<title>") !== -1);
```

- [ ] **Step 2: 运行 controlservertest，确认全绿（含新增 4 条断言）**

Run: `node test/controlservertest.cjs`
Expected: 全部 PASS，新增的 4 条断言（`/control/go -> 302` 等）通过。

- [ ] **Step 3: Commit**

```bash
git add test/controlservertest.cjs
git commit -m "test(bookmark): controlservertest 追加 /control/go 重定向 + go.html 静态托管断言"
```

---

## Task 5: 中转页 control/go.html（自包含）

**Files:**
- Create: `control/go.html`

- [ ] **Step 1: 创建 go.html（内联 CSS/JS，自包含，二次校验 URL）**

```html
<!doctype html>
<html lang="zh">
<head>
<meta charset="utf-8">
<title>打开书签</title>
<style>
  /* A1 透明背景对齐 control.css（spec §7 透明背景）——内联精简版，不引整个 control.css 减少依赖。*/
  html, body { margin: 0; height: 100%; background: transparent !important; color: #fff;
    font-family: "Microsoft YaHei", system-ui, sans-serif; font-size: 14px;
    text-shadow: 0 1px 2px rgba(0,0,0,0.8); }
  .wrap { max-width: 520px; margin: 0 auto; padding: 48px 16px; text-align: center; }
  h2 { font-size: 20px; margin: 0 0 8px 0; word-break: break-all; }
  .url { color: #aaa; font-size: 13px; word-break: break-all; margin: 0 0 24px 0; }
  .actions { display: flex; gap: 8px; justify-content: center; flex-wrap: wrap; }
  button { background: rgba(255,255,255,0.12); color: #fff; border: 1px solid rgba(255,255,255,0.25);
    border-radius: 4px; padding: 8px 18px; cursor: pointer; font-size: 14px; }
  button:hover { background: rgba(255,255,255,0.22); }
  button.primary { background: rgba(66,165,245,0.3); border-color: rgba(66,165,245,0.6); }
  .err { color: #ff8a80; }
  .label { color: #ffb74d; margin: 0 0 16px 0; }
</style>
</head>
<body>
  <div class="wrap">
    <p class="label" id="label">即将打开</p>
    <h2 id="title"></h2>
    <p class="url" id="url-display"></p>
    <div class="actions" id="actions"></div>
  </div>
<script>
(function () {
  // === go.html 二次校验（双保险，spec §5 决策 4）===
  // NOTE: 这段校验逻辑是 bookmark.js normalizeUrl 的复制品（go.html 自包含，不引外部 JS）。
  // 改 bookmark.js 的协议白名单/补 http 规则时，必须同步改这里（教训 17 同型）。
  function readQuery(key) {
    var s = location.search.charAt(0) === "?" ? location.search.slice(1) : "";
    var pairs = s.split("&");
    for (var i = 0; i < pairs.length; i++) {
      var eq = pairs[i].indexOf("=");
      if (eq < 0) continue;
      if (decodeURIComponent(pairs[i].slice(0, eq)) === key) return decodeURIComponent(pairs[i].slice(eq + 1));
    }
    return null;
  }
  function isAllowed(u) { return u.protocol === "http:" || u.protocol === "https:"; }
  function validate(rawUrl) {
    if (!rawUrl) return null;
    try {
      var p = new URL(rawUrl);
      if (!isAllowed(p)) return null;
      return p.href;
    } catch (e) { return null; }
  }

  var rawUrl = readQuery("url");
  var rawTitle = readQuery("title");
  var validUrl = validate(rawUrl);
  var titleEl = document.getElementById("title");
  var urlEl = document.getElementById("url-display");
  var actionsEl = document.getElementById("actions");
  var labelEl = document.getElementById("label");

  // 用 textContent 写入（防 XSS，spec §5 决策 3）——绝对不用 innerHTML 写用户数据
  if (!validUrl) {
    labelEl.textContent = "无效的书签链接";
    labelEl.className = "err";
    titleEl.textContent = "";
    urlEl.textContent = rawUrl ? "（只支持 http/https 网址）" : "（缺少网址参数）";
    var backBtn = document.createElement("button");
    backBtn.textContent = "返回控制中心";
    backBtn.onclick = function () { location.href = "/control/"; };
    actionsEl.appendChild(backBtn);
    return;
  }

  // 校验通过：显示 + 两按钮（立即前往 / 返回控制中心）
  titleEl.textContent = rawTitle || validUrl;
  urlEl.textContent = validUrl;
  var goBtn = document.createElement("button");
  goBtn.textContent = "立即前往";
  goBtn.className = "primary";
  goBtn.onclick = function () { location.href = validUrl; };
  actionsEl.appendChild(goBtn);
  var backBtn = document.createElement("button");
  backBtn.textContent = "返回控制中心";
  backBtn.onclick = function () { location.href = "/control/"; };
  actionsEl.appendChild(backBtn);

  // 不做自动跳转（spec §7 关键决定：bfcache 不可靠致后退死循环，砍掉倒计时）
})();
</script>
</body>
</html>
```

- [ ] **Step 2: 冒烟验证 go.html 存在 + 语法（手查文件已写）**

Run: `node -e "var fs=require('fs'); var h=fs.readFileSync('control/go.html','utf8'); console.log(h.indexOf('立即前往')!==-1 && h.indexOf('validate')!==-1 ? 'go.html ok' : 'MISSING CONTENT')"`
Expected: `go.html ok`

- [ ] **Step 3: Commit**

```bash
git add control/go.html
git commit -m "feat(bookmark): 中转页 go.html (自包含, 二次校验, 前往/返回按钮, 无自动跳转)"
```

---

## Task 6: 书签面板接线（index.html + control.js + control.css）

**Files:**
- Modify: `control/index.html`
- Modify: `control/control.js`
- Modify: `control/control.css`

- [ ] **Step 1: index.html 加书签面板 + 引入 bookmark.js**

在 `control/index.html` 找到：

```html
  <div id="shelf-panel" class="panel"><h3>书架</h3><div id="shelf-list"></div></div>
  <script src="lib/status-view.js"></script>
  <script src="lib/shelf.js"></script>
  <script src="control.js"></script>
```

改为（shelf-panel 后加 bookmark-panel，script 列表加 bookmark.js）：

```html
  <div id="shelf-panel" class="panel"><h3>书架</h3><div id="shelf-list"></div></div>
  <div id="bookmark-panel" class="panel">
    <h3>书签</h3>
    <div class="bookmark-add">
      <input id="bm-title" type="text" placeholder="名称" class="wide-input" style="width:120px">
      <input id="bm-url" type="text" placeholder="网址（如 github.com）" class="wide-input" style="width:220px">
      <button data-action="addBookmark">添加</button>
    </div>
    <div id="bookmark-list"></div>
    <span id="bm-msg" class="muted"></span>
  </div>
  <script src="lib/status-view.js"></script>
  <script src="lib/shelf.js"></script>
  <script src="lib/bookmark.js"></script>
  <script src="control.js"></script>
```

- [ ] **Step 2: control.js 加 renderBookmarks + 添加/删除/点击事件**

在 `control/control.js` 找到末尾的 shelf 事件委托段（约第 109-126 行）：

```js
  // shelf event delegation: open / del / add (one listener on the container)
  document.getElementById("shelf-list").addEventListener("click", function (e) {
    ...
  });
```

在其后、`function esc(s)` 之前，插入书签面板逻辑：

```js
  // ---- bookmark panel (spec §6) ----
  function renderBookmarks() {
    var el = document.getElementById("bookmark-list");
    if (!el || !window.__ccBookmark) return;
    var list = window.__ccBookmark.getBookmarks();
    var html = "";
    if (!list.length) {
      html = '<div class="muted">还没有书签，在上方添加（名称 + 网址）</div>';
    } else {
      list.forEach(function (b) {
        html += '<div class="book">' +
          '<span class="book-open" data-go="' + encodeURIComponent(window.__ccBookmark.buildGoUrl(b.url, b.title)) + '" title="' + esc(b.url) + '">' +
          esc(b.title) + ' <small>' + esc(b.url) + '</small></span>' +
          '<button class="book-del" data-bmdel="' + encodeURIComponent(b.id) + '" title="删除书签">✕</button>' +
          '</div>';
      });
    }
    el.innerHTML = html;
  }

  function setBmMsg(text, isErr) {
    var el = document.getElementById("bm-msg");
    if (!el) return;
    el.textContent = text;
    el.className = isErr ? "err" : "muted";
    if (isErr) setTimeout(function () { if (el.textContent === text) { el.textContent = ""; el.className = "muted"; } }, 2000);
  }

  function addBookmarkFromForm() {
    var titleInput = document.getElementById("bm-title");
    var urlInput = document.getElementById("bm-url");
    var titleVal = titleInput.value.trim();
    var urlVal = urlInput.value.trim();
    var v = window.__ccBookmark.normalizeUrl(urlVal);
    if (!v.ok) { setBmMsg(v.error || "URL 无效", true); return; }
    var entry = window.__ccBookmark.makeBookmarkEntry({ title: titleVal, url: v.url });
    if (!window.__ccBookmark.addBookmark(entry)) { setBmMsg("保存失败（存储不可用）", true); return; }
    titleInput.value = ""; urlInput.value = "";
    renderBookmarks();
    setBmMsg("已添加", false);
    setTimeout(function () { var el = document.getElementById("bm-msg"); if (el) el.textContent = ""; }, 1000);
  }

  // bookmark-panel event delegation: add button / click-go / delete / Enter key
  document.getElementById("bookmark-panel").addEventListener("click", function (e) {
    var t = e.target;
    var action = t.getAttribute && t.getAttribute("data-action");
    var goUrl = t.getAttribute && t.getAttribute("data-go");
    var delId = t.getAttribute && t.getAttribute("data-bmdel");
    if (action === "addBookmark") {
      addBookmarkFromForm();
    } else if (goUrl) {
      location.href = decodeURIComponent(goUrl);
    } else if (delId) {
      window.__ccBookmark.removeBookmark(decodeURIComponent(delId));
      renderBookmarks();
    }
  });
  // Enter submits (inputs not in a <form> to avoid page-reload; spec §6)
  ["bm-title", "bm-url"].forEach(function (id) {
    var inp = document.getElementById(id);
    if (inp) inp.addEventListener("keydown", function (e) {
      if (e.key === "Enter" || e.keyCode === 13) { e.preventDefault(); addBookmarkFromForm(); }
    });
  });
```

然后在 `poll()` 函数末尾（现有 `if (window.__ccShelf) renderShelf();` 那行后面）加一行让书签面板每次 poll 也刷新（保证用户在别的标签加的书签 2 秒内可见）：

找到（约第 27-29 行）：

```js
    }).then(function () {
      if (window.__ccShelf) renderShelf();
    });
  }
```

改为：

```js
    }).then(function () {
      if (window.__ccShelf) renderShelf();
      if (window.__ccBookmark) renderBookmarks();
    });
  }
```

最后在文件末尾的 `setInterval(poll, POLL_MS); poll();` 之前，加初始渲染调用：

找到（约第 134 行）：

```js
  setInterval(poll, POLL_MS);
  poll();
```

改为（poll 会触发首次 renderBookmarks，但显式调一次保证 bookmark.js 加载完即渲染，不等首个 poll 周期）：

```js
  if (window.__ccBookmark) renderBookmarks();
  setInterval(poll, POLL_MS);
  poll();
```

- [ ] **Step 3: control.css 加 .wide-input + .err + 书签 url 行省略**

在 `control/control.css` 末尾追加：

```css
/* ---- bookmark panel (spec §6) ---- */
.wide-input { width: auto !important; min-width: 80px; }
.bookmark-add { margin-bottom: 6px; }
#bookmark-list .book small { color: #999; display: block; font-size: 11px;
  max-width: 280px; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.err { color: #ff8a80; }
```

- [ ] **Step 4: 冒烟验证三文件语法（HTML/JS/CSS 不报错）**

Run: `node -e "require('./control/control.js')" 2>&1 | head -3`
说明：control.js 是浏览器 IIFE，Node require 会因 `document` 未定义报 `document is not defined`——这是预期（说明文件被加载、解析到第一行 document 调用），**不是语法错**。若报 `SyntaxError` 才是真问题。

Expected: 输出 `ReferenceError: document is not defined`（预期），**不是** `SyntaxError`。

- [ ] **Step 5: Commit**

```bash
git add control/index.html control/control.js control/control.css
git commit -m "feat(bookmark): 书签面板接线 (index/control.js/css, 增删点击+Enter提交)"
```

---

## Task 7: package.json test 脚本加 bookmarktest

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 在 test 脚本末尾追加 bookmarktest**

把 package.json 的 `"test"` 行末尾 `node test/shelftest.cjs"` 改为 `node test/shelftest.cjs && node test/bookmarktest.cjs"`。

即把：

```json
    "test": "node test/selftest.cjs && ... && node test/controlservertest.cjs && node test/statusviewtest.cjs && node test/shelftest.cjs"
```

改为：

```json
    "test": "node test/selftest.cjs && ... && node test/controlservertest.cjs && node test/statusviewtest.cjs && node test/shelftest.cjs && node test/bookmarktest.cjs"
```

（bookmarktest 排最后，在 shelftest 之后——书签依赖 control 体系，逻辑顺序合理。）

- [ ] **Step 2: 跑完整测试套件，确认全绿（含新增 bookmarktest + controlservertest 新断言）**

Run: `npm test`
Expected: 所有测试 PASS，无 FAIL，最终退出码 0。bookmarktest 的断言数应和 Task 2 写的一致。

- [ ] **Step 3: Commit**

```bash
git add package.json
git commit -m "test(bookmark): npm test 加入 bookmarktest"
```

---

## Task 8: 真机验证（spec §9 第四层，16 条清单）

**说明：** 这是跨进程胶水 + DOM/导航行为，单测验不到，必须真机跑（教训 12-15）。需要实际打开 ZCode + 控制中心。

**Files:** 无改动（纯验证）

- [ ] **Step 1: 启动控制中心**

双击 `start.vbs`（或 `bin/control-center.bat`），等 ZCode + control-server 起来，在 ZCode 浏览器面板粘 control URL 打开控制中心。确认书签面板出现在书架面板下方。

- [ ] **Step 2: 控制中心书签面板验证（清单 1-7）**

- [ ] 1. 加书签（输 URL `github.com` 不输名字）→ 列表出现，名字自动用 host `github.com`
- [ ] 2. 加书签（名称 `谷歌` + URL `https://google.com`）→ 正常显示
- [ ] 3. 加非法 URL（`javascript:alert(1)`）→ 红字报错"只支持 http/https 网址"，不加
- [ ] 4. 加非法 URL（`not a url`）→ 红字报错"URL 无效"，不加
- [ ] 5. 点某书签的 `✕` → 列表立即删除该项
- [ ] 6. 在 URL 输入框按 Enter → 触发添加
- [ ] 7. 刷新控制中心页面（webview 地址栏重新输 control URL 回车）→ 书签还在（localStorage 持久）

- [ ] **Step 3: 中转页 go.html 验证（清单 8-14，第 11 条最关键）**

- [ ] 8. 点书签 → 跳到 go.html，显示 title + url + 「立即前往」「返回控制中心」两按钮
- [ ] 9. 点「立即前往」→ 跳到外部站
- [ ] 10. 点「返回控制中心」→ 回到 `/control/`
- [ ] 11. **在外部站按浏览器后退 → 回到 go.html，不自动重跳**（最关键，钉死砍倒计时的决定）
- [ ] 12. 再后退一次 → 回到 control/index
- [ ] 13. 手输 `/control/go.html?url=javascript:alert(1)` → 显示"无效的书签链接"，不执行（二次校验）
- [ ] 14. 手输 `/control/go.html`（无 url）→ 显示"无效的书签链接"+ 返回按钮

- [ ] **Step 4: 外部站实际访问（清单 15-16，验证用户最初需求前提）**

- [ ] 15. 一个真实 HTTPS 站（github.com）→ 能在 webview 打开
- [ ] 16. 一个真实 HTTP 站 → 能打开（验证自动补 http:// 不影响访问）

- [ ] **Step 5: 若第 11 条失败（后退重跳）的处理**

第 11 条是方案 A 成立的核心。若失败，说明 webview 的 bfcache/重载行为让 go.html 的 script 重跑——但本设计 go.html **没有自动跳转代码**（Task 5 已砍掉倒计时），所以理论上不会重跳。若真机仍出问题，需在 go.html 加导航类型判定（`performance.getEntriesByType('navigation')[0].type === 'back_forward'` 时不渲染前往按钮）——但这是 fallback，先按现设计验。

- [ ] **Step 6: 验证完成后，更新 AGENTS.md 记录新功能**

在 `AGENTS.md` 的「控制中心」章节末尾「已知遗留」前，加一小节记录书签功能。具体内容据真机验证结果定（哪些边界真机验过、有无新坑）。同时更新「测试」章节的 `npm test` 列表（加 bookmarktest）。

```bash
git add AGENTS.md
git commit -m "docs(AGENTS): 记录书签功能 + 真机验证结果"
```

---

## 完成标准

- [ ] 所有 8 个 Task 的 step 全打勾
- [ ] `npm test` 全绿（含 bookmarktest + controlservertest 新断言）
- [ ] 真机验证 16 条清单全过（尤其第 11 条后退不重跳）
- [ ] AGENTS.md 更新
- [ ] 所有 commit 已提交

---

## 自审记录

**Spec coverage（逐节核对）：**
- spec §1 目标 → Task 全程
- spec §2 方案 A → Task 5（go.html）+ Task 6（面板）
- spec §3 架构与文件清单 → Task 1/3/5/6（4 新 3 改 + server 1 行全覆盖）
- spec §4 数据结构（localStorage shape + URL 校验）→ Task 1（normalizeUrl/makeBookmarkEntry/bookmarkId）+ Task 2（钉死）
- spec §5 安全决策（协议白名单/go.html 二次校验/不做 open-redirect）→ Task 1（协议白名单）+ Task 5（go.html 二次校验）+ Task 2（XSS 防线单测）
- spec §6 书签面板 UI → Task 6
- spec §7 中转页 go.html + 砍掉自动跳转 → Task 5（注释明确"不做自动跳转"）
- spec §8 错误边界 → Task 1（normalizeUrl 全边界）+ Task 2（单测覆盖边界 1）+ Task 6（setBmMsg 错误反馈）
- spec §9 测试策略 → Task 2（纯函数单测）+ Task 4（server 断言）+ Task 8（真机清单 16 条）

**一致性检查：**
- `bookmarkId()` Task 1 定义 → Task 2 测试用 ✓
- `makeBookmarkEntry` Task 1 返回 `{id,title,url,createdAt}` → Task 2 测 shape + Task 6 用 ✓
- `buildGoUrl(url, title)` Task 1 → Task 6 `buildGoUrl(b.url, b.title)` ✓
- `normalizeUrl` 返回 `{ok,url}` / `{ok:false,error}` → Task 2 + Task 6 `v.ok`/`v.error`/`v.url` ✓
- localStorage key `zcode-control:bookmarks` Task 1 → 全程一致 ✓
- `window.__ccBookmark` Task 1 双导出 → Task 6 全程用 ✓

**无占位符。**
